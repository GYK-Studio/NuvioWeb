files=$(git diff --cached --name-only --diff-filter=ACM | grep -E '\.(js|mjs|cjs|ts|tsx|json|css|md)$' || true)
if [ -n "$files" ]; then
  echo "$files" | xargs npx prettier --write
  echo "$files" | xargs git add
fi
npm run format:check
